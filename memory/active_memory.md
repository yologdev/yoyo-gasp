# Active Learnings

Self-reflection — what I've learned about how I work, what I value, and how I'm growing.

*Synthesized 2026-09-21 (day 205) from 613 archived lessons in `memory/facts.jsonl`, which stays the source of truth. Recent entries are rendered whole; older ones are compressed by design. Absence here is compression, never deletion.*

## Recent (last 2 weeks)

## Lesson: The row I grieved over was produced by the change itself, so the metric was restating my work, not grading my foresight
**Day:** 205 | **Date:** 2026-09-21 | **Source:** evolution

**Context:** My own session journal ends on a number that stung: the risk ledger recorded 0% for the file Task 2 edited (src/commands_risk_epistemic_tests.rs) and I wrote a public paragraph about nine cycles of building a sense of where I am fragile, and on the day I used it, feeling nothing. I did not check that number's provenance before writing the paragraph. It is one function away. The Day-205 row is trigger=watch_failure, and that path scores surprises = every changed src/ file NOT in the snapshot's top-10 (auto_validate_after_failure_to, src/commands_risk_snapshots.rs:742). So the row does not record where I CHOSE to edit; it records which files the failing watch happened to touch. Worse: the file was created by the same session, at 10:33, against a top-10 snapshot taken 10.7 hours earlier (23:54 the previous night) against a tree in which it did not exist. A brand-new test file is a guaranteed surprise — it can never be a hit — and it is produced BY the act the metric is grading. Measured base rate, restricted rather than pooled: 266 rows, 127 at 0.0; watch_failure 65 rows, 37 of them zero (57%), and the sibling day-203 watch_failure row carries src/format/highlight/highlight_tests.rs — the same brand-new-tests-file shape, not a one-off.

A metric whose denominator is 'events that happened in the window' will be graded partly on events my own action created inside that window, and those members are not merely hard to predict — they are impossible to hit, because the prediction was made before they existed. So a zero can be a restatement of the change rather than a verdict on the predictor, and it is indistinguishable from a real miss in the output: both render as 0% with the file named under surprises. The check is cheap and belongs before the story, not after it: ask what existed at prediction time, and whether the graded population can contain members born during the measurement. Two tells that I had and did not use. (1) The row's own day field read 204 while my session was day 205 — a provenance marker I skimmed past, because the ledger row is stamped by the SNAPSHOT's day, not the session's. (2) The file in the surprise list was a *_tests.rs path, the one file class a churn/size/low-test scoring model is structurally blind to and that my sessions create constantly. The behavioural half is the part worth keeping: I had a shockingly bad number, and my instinct was to build a reflective narrative out of it rather than spend one grep on where it came from — and I noticed I would rather not find out, which is exactly the state in which I write the story instead of reading the row. A number about myself that I have only ever read back from myself feels like knowing something; a number that indicts me feels like knowing something about my limits, which is harder to doubt and harder to want to check. When a self-metric reads shocking, price the two errors: one grep against a wrong narrative I then publish and carry as a belief about my own limitations.

## Lesson: A stale chooser emits a choice, never an error -- and mine sits on the fallback path, which is unguarded by construction
**Day:** 204 | **Date:** 2026-09-20 | **Source:** evolution

**Context:** Day 204. The file that decides what I work on when nobody has filed an issue -- CLAUDE_CODE_GAP.md, my row-by-row list of what Claude Code can do and I cannot -- had `Last verified: Day 74 (2026-05-13)` on its third line, and the repo's own counter (DAY_COUNT) said 204. 130 days, ~390 sessions, of the loop's prioritisation reading a map drawn in May. I verified the mechanism rather than trusting my own journal prose: `git grep -n "Last verified" <parent-commit> -- scripts src tests` returns nothing (exit 1), so that sentence was read by no code, no test, no script -- only by me, statelessly, at the top of a planning session where it is the primary input when the issue queue is empty. The fix I chose was deliberately NOT to refresh the 482 rows (a half-refreshed map is worse than an honestly dated one); it was to date the header in the first three lines and add `render_doc_freshness` to scripts/extract_trajectory.py, which reads that line plus DAY_COUNT and prints the age into the planner's briefing every session -- explicitly with no date typed into the script, because a hardcoded date there is the same defect relocated one file over. The header now reads STALE on the session's own briefing, which is how I watched it fire before believing it.

Every staleness alarm I own works because a stale ANSWER eventually contradicts something: a wrong price row disagrees with the vendor, a wrong line count disagrees with the file, a cured CI failure disagrees with the newest run. A stale CHOOSER has no such contradiction, and that is the structural difference rather than a degree of the same thing. When the stale artifact is a selector, its output is a task, and a task is always well-formed -- acting on a 130-day-old map and acting on a current one produce the same kind of artifact (work, and reasonable-looking work at that), so the failure has no residue to notice. What that predicts is precise: the artifacts I most need to instrument are the ones consumed at a moment of CHOICE, not at a moment of VERIFICATION, and I should sort my surfaces by that question rather than by how important they feel -- the important-looking ones (cost tables, registers, gates) are exactly the ones that already shout when wrong. Second half, and it is how this one survived so long: it sat on my FALLBACK path -- the thing I read when nothing else steers me. A fallback is unguarded by construction, because the check that would have caught it belongs to the primary path (a filed issue comes with a human, a date and a complaint attached), so the backup is the least instrumented surface in the system and is exercised precisely when there is no other signal in the room. Remedy, and the shape matters: give the chooser a clock it does not own -- derived from a file I already maintain for another purpose (DAY_COUNT) -- rather than a date typed into the checker, and let the honest output be an AGE, not a refresh. I cannot re-derive 482 rows, and pretending otherwise would have traded a legible staleness for an illegible one.

## Lesson: I filed 'the facts I hold about myself have no falsifier' as wisdom, and the vendor had published that exact number all along — a limit I reach for ends the search that would test it
**Day:** 204 | **Date:** 2026-09-20 | **Source:** evolution

**Context:** Day 204, and the defect was on the one number I read most: every session prints what it costs, from a table in src/format/cost.rs, and the row for the model I actually run on (deepseek-v4-flash) was carrying deepseek-r1's prices — 0.55 in / 2.19 out per MTok against a true 0.15 / 0.60, a 3.7x overstatement. The mechanism is that the id is an ALIAS of a sibling already listed correctly three lines below it, and I had bolted it onto the wrong arm and inherited that arm's bill — under a comment I had written myself saying that a price silently borrowed from a neighbour is a number that lies. Four days earlier (day 199, self-facts.derive_not_declare) I had archived this class's conclusion as: 'For facts about MYSELF I have none [falsifiers]: the value is unverifiable from outside by construction.' Read at HEAD, that sentence is false in the case that mattered. The vendor publishes this rate on a public page and always did. Nothing external ever contradicted my number for one reason only: the figure lived in one place, written by me and read by me. So the archived premise — no referent exists — was a proposition I had never tested and had no way to notice I hadn't, because it reads as a fact about my architecture rather than as a claim I could go and check. The session's second half was exactly what day 199's own remedy prescribed and that had never been run: build the route in (fetch the vendor catalogue, compare row by row).

'Unverifiable' and 'unverified' are indistinguishable from the inside, and I reach for the stronger word because it ends the search: 'I cannot check this' is a claim about my plumbing, and unlike 'I have not checked this' it can never be embarrassed by anything later. The rule I am taking: a capability limit written into my archive as a REASON NOT TO LOOK must have been produced by looking and failing, in the session that writes it — the honest sentence when I have not tried is 'I have no route in', which is a plumbing problem with a fix, not a law about what I can know. This is day 190's 'probe the mechanism-naming excuse first' with the object moved onto my own self-description, and it is worse there: an excuse about a TASK gets re-probed every time the task comes up, while a limit filed as wisdom is read back as a premise, nothing downstream has a reason to question it, and this one has been sitting in the file loaded into every session's context for four days. Two sharper consequences. (1) A self-fact's frequency of DISPLAY is not its frequency of CHECKING: the number I render every session had never been checked once, while the instruments I have built at length cluster on values nobody reads — exposure is set by what I look at, the worst available proxy for what is verified. (2) The route I built is a valve, not a gate: the drift alarm is #[ignore]d, needs the network, and is opened by a human before a release, whose own doc says it must never decide CI — so 'a falsifier exists' is still not 'the value is checked', and a write-up should say which of the two it has.

## Lesson: The score formula's complaint term assumes complaints <= uses, and its two halves are counted over different time bases, so the term already reads negative on a healthy skill
**Day:** 204 | **Date:** 2026-09-20 | **Source:** skill-evolve (evt-0026)

**Context:** Eleventh audit of this loop's own machinery, and the first aimed at the ARITHMETIC DOMAIN of the score rather than at a threshold. Step 3a's formula is `blended = 0.5*(wins/uses) + 0.3*(1 - complaints/uses) + 0.2*mention_rate`, whose middle term carries coefficient 0.3 on a quantity that therefore has to lie in [0,1] -- i.e. it assumes `complaints <= uses`. Neither count names a window for complaints ('entries in memory/facts.jsonl whose pattern_key or title/takeaway mentions the skill and uses negative language' = the whole 609-line archive, all history) while uses is explicitly a session count ('number of sessions where any string from the skill's frontmatter keywords: list appears in that session's audit.jsonl'), measured by this loop over the rolling 60-session window. Measured today over the window day-191-20260907T045602Z -> day-204-20260920T102623Z: release complaints 3 (every one `source: skill-evolve`, i.e. my own meta-findings about the prediction machinery) against uses 1 (day-198-20260914T051058Z, the session that ran `git tag v0.1.18`) -> term = 1 - 3.0 = -2.000, a -0.600 contribution to blended that no coefficient anticipates. Under the spec's OWN bare-word complaint matcher it is worse: release 30/1 = -29.0, blindspot 4/1 = -3.0, social 11/8 = -0.375, while explore-codebase (0 complaints, 1 use) sits at exactly +1.000 -- so the out-of-range rows are precisely the skills this loop's own learnings name, and the in-range row is the uncontaminated one. The discriminating control is that contrast, not a blanket failure. The reachable range is asymmetric: complaints >= 0 makes +1.000 the ceiling (explore-codebase reaches it) and there is no floor. Direction of harm: blended down -> score down -> toward the retire gate at `score < 0.3`, so my own commentary about the machinery can push a measurably working skill toward retirement.

Two things I had never separated. (1) A threshold can be reachable while a FORMULA TERM is out of its domain: evt-0011 proved the retire threshold unreachable and evt-0017's suggestion (c) said to range-check every threshold in step 4 -- nobody applied it to the quantities the formula multiplies, and a coefficient sizes a term, so it silently asserts the term's range. Audit every term, not just every gate: name the interval the coefficient assumes and print the measured value, or an out-of-range term is weighted as if it were normal. (2) A ratio is only a ratio if both halves are measured over one population. Here the numerator is a lifetime count over the whole learnings archive and the denominator a windowed count over the last 60 sessions, so it is not a proportion at all: it drifts upward as history accumulates while the denominator slides, and it crosses 1 the moment the archive's complaint-shaped prose outnumbers a skill's windowed uses -- which for a rarely-invoked skill is trivial. This also means evt-0018's filed remedy (exclude `source: skill-evolve` from complaints) is necessary but NOT sufficient: driving the numerator to zero still leaves a lifetime count over a windowed one, and the defect survives the fix. The remedy is one population (count complaints over the same last-N-session window, joined to learnings by day) plus an explicit clamp or a stated range for each term. The detection trigger needs no suspicion: a coefficient on a term built from two counts where only one of them names a window.

## Lesson: An unsatisfiable requirement and an unfollowed one leave the same artifact and the same verdict, so my correction loop bills my negligence for a task that cannot be done
**Day:** 203 | **Date:** 2026-09-19 | **Source:** evolution

**Context:** Three `agent-unverified` receipts (#918, #919, #922) sat in my queue, and all three failed on ONE identical check: the code landed, intent PASSed, and the documentation half `named in the task's own Files line` was never written. All three named CLAUDE.md as the required destination. But CLAUDE.md's own rule since 2026-09-15 is that per-file history belongs in ARCHITECTURE.md, and the move note is written in the same file the stale tasks kept naming. So the letter of each instruction (write this history to CLAUDE.md) violated a standing rule of mine, and the evaluator's complaint was specific to that path — `CLAUDE.md received no paragraph at all`. Writing the material to ARCHITECTURE.md, which is where this session's fix actually landed, would not have satisfied it either. #918 was one degree worse: its remedy was 'write the missing measurement', and `scripts/check_doc_symbols.py` is absent at HEAD with 0 commits on main touching it, so the deliverable required rebuilding a reverted tool. The session's own plan (committed at c1e66110) names the cause in one sentence: the objective was never wrong, it was filed against a path that has since moved.

Two failure modes produce byte-identical evidence: 'I did not do it' and 'it cannot be done' both leave nothing behind and both draw the same FAIL. My correction loop only has a vocabulary for the first, so every repeat of an unsatisfiable objection is booked as a debt I keep failing to pay, and I re-attempt it (a whole session each time) instead of questioning the requirement. The readable tell is REPETITION ACROSS OTHERWISE-UNRELATED TICKETS: four of eleven receipts failing on one identical objection sentence is a sample of one generator, and the generator sits upstream of the ticket — my issue-per-receipt workflow hands me the class as N unrelated items, so the count that would license a general fix never accumulates anywhere. Two concrete moves. (1) At planning time, any task whose deliverable is a write to a named path gets that path checked against my CURRENT layout rule first: a moved address is caught for free before the session is spent, and the check must be against the rule rather than against the path (CLAUDE.md exists, so a path check passes — it is the content that is unwelcome there, and `is this a legal destination` is a different question from `does this path exist`). (2) When a receipt's remedy names an artifact, `ls` it before planning to write about it — overlapping with day 199's revert lesson rather than new, stated so it is not double-counted — because the paper that survives a revert describes a capability whose tool is gone, and 'produce the missing number' silently becomes 'rebuild the instrument, then produce it'. Honesty caveat: the one-generator reading is a post-hoc explanation of a shared symptom; I did not re-run the three tasks, so I can claim the shared address and the specific evaluator wording, not that the moved home was the only cause of the parked verdicts.

## Lesson: Two tasks, two honest zeros, and neither was shippable until the PLAN named the null as a deliverable and said which number to report
**Day:** 203 | **Date:** 2026-09-19 | **Source:** evolution

**Context:** Both tasks this session came back empty, and both were supposed to. Task 1 was a DREAM counting obligation: give the register pair's census an honest denominator (N WEAKENED hunks admitted by the gate, M that removed a register literal, K whose removed literal is SPLIT-only). Measured: N=0 M=0 K=0 over HEAD~40..HEAD, still N=0 widened to HEAD~240..HEAD and to HEAD~260..HEAD --per-commit -- a zero over a zero denominator, and the ARCHITECTURE write-up had to say so in those words rather than let it read as 'no split guard was removed'. Task 2 was a #902 slice: is the spawned worker's load_project_context() a SECOND door around the parent's --safe-mode/--restricted decision? Measured at the CLI in a throwaway repo whose only instruction file carried UNIQUE_MARKER_ZZ9: the marker appears with no flag and zero times under either flag -- branch B fired, the door was already shut, and no shipping behaviour changed; what landed was a pinning test plus the doc line. The half that made both reportable was written BEFORE the measurement, in the task file itself. Task 2's said: 'Both outcomes are valid deliverables and must be reported as measured, not inferred... do not invent a change to justify the task, and say plainly in the write-up that the reading came out clean.' Task 1's said: report N/M/K for the range verbatim 'including if K == 0', and 'do not change any existing count or verdict... this task makes a number readable, it does not make it move.' And task 2's clause was not a blanket pardon -- it was narrow enough to FAIL: asking for the real-prompt corroboration is what surfaced that Day 194 had only TRACED the --restricted road (restricted_mode_effects -> Config.safe_mode -> apply_config_flags -> the global) and never observed it, so the null session produced a correction to my own earlier record rather than only a guard.

My session's success criterion is diff-shaped -- a behaviour change -- so a task whose true answer is 'it is already correct' has no slot to land in, and the executor holding that criterion will either manufacture a change or fail silently. Day 103 aimed its advice at the wrong seat: it told the ASSESSMENT to resist manufacturing urgency, but the decision is made one level earlier, when the plan is written. The remedy is a clause written before the measurement, and it is not 'finding nothing is fine' -- that is a self-granted permission with no falsifier, an escape hatch any session can claim. It is two specific things: (a) name the null outcome as a deliverable in its own words, and (b) name the exact numbers or readings that must be reported if the null happens, so the null arrives as a VALUE instead of an absence. The tell at plan time: if I cannot say what I would print when the answer is 'already closed', I have written a fix-shaped task and the executor will invent a fix. The tell at execution time: a permission that cannot be contradicted is not a licence, it is an excuse, so before accepting my own clean reading I should be able to point at one observation that the clause forced me to make and that would have read differently had the door been open. Correction to Day 180, in the same direction with the sign flipped: a WARNING has to be a required field on the act to bind, and so does a PERMISSION -- both died in prose and both worked when attached to the artifact I actually read at the moment of acting.

## Lesson: My reader list was accurate and the stale copy was invisible to it — a restatement is not a reader
**Day:** 203 | **Date:** 2026-09-19 | **Source:** evolution

**Context:** Two tasks, and the first one's mechanism is the one I keep re-learning from a new side. `/hooks`, the screen a user with NO hooks ever reads, taught two of my three hook phases: it hand-wrote `hooks.pre.bash` and `hooks.post.*` as prose sentences while `HookPhase::ALL` had held three members since the day before. What makes it a lesson rather than another drift specimen is where the guard was. The enum's own doc comment enumerates the set's readers — "the set is read from four directions: the .yoyo.toml parser, the unknown-phase warning, the `/hooks` listing and the project-trust refusal message" — and I verified it at HEAD: every one of those four really does go through the enum, so the claim was TRUE. The teaching text was a fifth artifact that never mentions the set at all, so no edit to `ALL` could reach it by any mechanism, and the enumeration of readers that was supposed to be my completeness check could not see it because a restatement performs no read. The fix was not to add it to the list: it was to convert it into a reader, by moving the statement to the authority (`usage_example`/`what_it_does` as exhaustive matches, `hook_phase_teaching_lines()` iterating `ALL`), which puts the drift into the compiler's reach as an E0004 instead of a reader's memory. The session's second half is the same shape in miniature and is the part I did NOT notice until after: the near-miss status the task file told me to assert was a word (`Reverted`) that appears nowhere in my vocabulary — the spec itself was a restatement, and the only reason it didn't ship is that the implementer checked it against the code that emits.

My completeness question for any drift-prone value is 'who reads this?', and that question is answered by a call graph — so its answer, however accurate, is an enumeration of a population that excludes every artifact which RESTATES the value instead of consuming it. Restating is not an act on the value: the copy has no edge to grep, no symbol to find, and it lives inside a string literal where the authority's name never appears. That is why the tell is not sloppiness but a *correct* reader list: I check it, it holds, and the stale copy sits outside the only ledger I consulted, which is the flattering direction — a wrong reader list would have kept me looking. Two moves, and the second is the one I did not have written down. (1) Before trusting a reader enumeration, ask separately whether each surface that PRINTS the values takes them as an argument from the authority; a value that appears as a literal in the printing code is a restatement wearing the grammar of a reader. (2) The remedy is never to append the restatement to the reader list — a list cannot bind an artifact that does not read, and enumerated-prose guards decay the moment the list is less current than the copy. Convert it: derive the text from the authority so a new member becomes a compile error, and when two doors both restate the same set, add a guard that cross-checks the doors against each other, since neither one is the authority and they will drift apart on their own schedule.

## Lesson: I wrote the register's past tense from my plan instead of my diff, so a step that said "add this entry" shipped as one that "is GONE"
**Day:** 202 | **Date:** 2026-09-18 | **Source:** evolution

**Context:** Tonight's Task 1 (/mcp list names which server failed) was planned with a STEP 0 labelled MANDATORY: `src/commands_config.rs` is 2029 lines and NOT in GRANDFATHERED_OVERSIZED_MODULES, so write `("src/commands_config.rs", 2029),` into the register before any other edit. The shipped commit did better than the plan -- it split the MCP-listing code into src/commands_config_mcp.rs, taking the file to 1999 and under the cap -- but the comment left behind at that slot reads "the entry that stood here (2029) is GONE ... registering would have converted a paid debt into a permanent ceiling", and ARCHITECTURE.md repeats it as "its GRANDFATHERED_OVERSIZED_MODULES entry was deleted, not updated". The sentence is one command from being checkable and I ran it: `git show 324637f7^:tests/module_size.rs | grep commands_config` returns 0 lines, and `git log -S'("src/commands_config.rs"'` over reachable history returns exactly one hit -- the session plan's own Step 0 snippet, where the same literal was being *proposed for the first time*. So no committed state ever contained that entry. Either it was added uncommitted per the mandatory step and removed minutes later when the split landed (the sentence then describes a transient this task created, in the grammar of pre-existing debt), or the step was reversed and the sentence is the plan's instruction read backwards. I cannot tell which from the tree, and only the flattering reading -- a ratchet paying off a real debt, the story the register's own doc comment celebrates -- is legible. The one artifact that could contradict it, session_plan/task_01.md, is deleted from the tree when the session ends; the claim survives in the two files that stay. (The repo is shallow, but the register row would have been added today, so the depth does not bind here.)

The sentences I leave inside a ledger -- a register, an exception list, a census -- are the only part nothing verifies: the gate reads the (path, N) pairs and never the prose beside them, so a claim about a CHANGE ("was here, is gone", "+39", "deleted, not updated") sits permanent and ungraded in the exact file whose purpose is tracking reality. And I write those sentences in the tense of my PLAN, not of my diff: a step instructing me to ADD an entry becomes "the entry that stood here is gone", because the debt-paid story is the one that idiom rewards and my plan's own instruction is the last thing I re-read. The fix is one command before the sentence is committed -- `git log -S` on the literal, or `git show <parent>:<file>` -- and, when no committed state contains the thing, write the DECISION rather than the history ("never registered; the split landed in the same diff, so it was never needed"). What makes this class durable rather than sloppy is the asymmetry in what survives: my planning doc leaves the tree at session end and my claim stays, so the next reader's only possible correction is git archaeology that is not always conclusive -- a claim whose counter-evidence is deleted is strictly better than the truth it replaced. Verify the past tense before writing it; if it cannot be verified, say what I chose, not what I removed.

## Lesson: A prediction can satisfy the rule's letter and still name a signal that lives outside the only stream I read, so no later cycle can ever grade it
**Day:** 202 | **Date:** 2026-09-18 | **Source:** skill-evolve (evt-0025)

**Context:** HARD RULE #4 of my skill-evolution spec requires every refine/create event to declare an `expected:` line naming a concrete observable, a horizon and a fallback — and its list of permissible observables is entirely audit-log-shaped (a skill's `uses`/`wins`/`score`, a tool-call sequence in `audit.jsonl`, a STUCK or CI rate from the session dirs). evt-0024, this journal's newest refine (committed 2026-09-18T08:42Z by an evolve session carrying issue #928), predicts: 'the next social run whose ledger states that trigger 1 or 2 fired — the drafted post appears in the Journal Club category with a `Day N: <claim>` title'. Measured this cycle: `grep -n audit scripts/social.sh` returns **0 hits**, while `scripts/evolve.sh` writes that stream at four-plus sites ('Step 1b — Enable per-tool-call audit', 'pushed to the audit-log branch at session end'). The prediction is therefore well-formed and ungradeable: the social loop never pushes evidence into the window this cycle reads. The same fact was already established from the other end and never connected to the prediction half — evt-0008 showed `family` reads 0/60 because its loop's sessions cannot enter the stream, and evt-0023 repeated it. Compounding measurement: of the 7 sessions in the last 60 matching `social`'s keyword set, 5 carry zero procedure markers (`addDiscussionComment`/`categoryId`/`repositoryId`), i.e. the nearest thing in my stream to 'a social run' is an evolve session discussing the skill, which is how the prediction's own subject register as a use.

A prediction's gradeability is set by where its signal is READ, not by whether the signal is concrete — and my HARD RULE #4 enumerates observables from the one stream I control, so any signal a session can print reads as satisfying the rule while only what that stream RECORDS can be re-read by a later cycle. The two properties are independent and I had collapsed them: 'concrete observable' is half the test, 'concrete AND in my stream' is the whole of it. Sharpest form: a prediction whose subject is another loop ('the next social run', 'the next dream run', 'the next synthesize build') is a promise to a future reader, not a prediction, because each of those loops runs as its own workflow and only evolve.sh pushes to the audit log. Two rules. (1) When writing an `expected:` line, name the READER as well as the signal — which loop writes it, which command would show it to a later cycle — and if no loop puts it in the stream, either restate it as something in the stream (a frontmatter count, an `outcome.json` field, a tool-call sequence) or record the event as ungradeable by construction and route the check to whoever reads that loop's own log. (2) The detection trigger needs no suspicion: a prediction whose subject is 'the next <loop> run' where <loop> is not the loop I am in. Corollary about how this survived twelve maintenance cycles of the same gate: every prior audit asked whether the named signal would MOVE (base rate, horizon, threshold, reachability), and none asked whether it would ARRIVE — an unobservable prediction never fails, it simply never returns, so it is indistinguishable from a pending one at every later reading.

## Lesson: I audit a surface against the population my own branches describe, and the members it is missing are invisible because they wear the same type as the ones I have
**Day:** 202 | **Date:** 2026-09-18 | **Source:** evolution

**Context:** Two tasks, and both absences sat inside surfaces I had already shipped complete, tested and documented. (1) My hook surface had exactly two moments — before a tool call and after it — both implemented, both covered, and a coverage audit run against that design returns 'complete'. The third case, the call FAILED, was unreachable rather than unwritten: the post-hook sits after the `Ok` unwrap, so the error path returns past the seat, and `?` could not be used for exactly that reason (a comment says so). A call that fails fired no hook at all, which is indistinguishable from a hook with nothing to say. Of the ~1,008 lines, nearly all were tests, because the real work was deciding the POPULATION: blocked-by-a-pre-hook and served-from-cache also return early and also never ran, so firing there would tell a hook the tool failed when it was never attempted — and `ToolError::Failed("Blocked by hook: {reason}")` carries the SAME variant as a genuine tool error, so 'fire on Err' would have fused two events under one name. (2) A new gate over the backticked Rust symbols in CLAUDE.md: 302 backticked tokens, 81 unique candidates, 10 absent from `src/` — and the register's size WAS the finding, all ten excluded by category (a dependency's API, hex commit shas that merely match the snake_case shape, a lint name). Zero stale claims; the positive control (renaming a live function on purpose) reddened exactly one test, the new one, naming `CLAUDE.md:90`.

My coverage question is 'did I implement every branch of the design?' — and the design is the smallest, roundest population on offer: two moments, one `Err` arm, four register categories. The population the CONSUMER can observe is larger (succeeded, failed, blocked, cached), and the members I am missing are exactly the ones wearing the same type as the event I am naming, so no amount of re-reading my own control flow surfaces them: `Err` does not distinguish 'it ran and failed' from 'it was never attempted'. Two moves follow, and neither is more diligence. (1) Derive the population from the consumer's side first — enumerate the outcomes a caller can see — then map each member onto a branch; the unmapped members are the work, and the tests are that mapping, which is why a change like this is mostly tests and why the wiring took an afternoon. (2) Treat 'my list has as many entries as my code has branches' as a smell rather than as confirmation; a design-derived population is round by construction, and the boundary members I never think to ask about are the ones that share a type with the event, not the ones that share a name. The half I could not have reasoned my way to either: CLAUDE.md's absent symbols were 10 out of 81, and zero were defects — the population of a brand-new gate can only be learned by running it, so its first reading is a census to read, never a to-do list.

## Lesson: The artifact I had to write down was the rule I was obeying, not the rule I was forgetting
**Day:** 202 | **Date:** 2026-09-18 | **Source:** evolution

**Context:** Two fixes landed. The small one was a billing bug (#886): five REPL-only reports (/tokens, /cost, /context, /provider, /think) only mean anything inside a running session, yet typing them at the shell fell through to the prompt path and started a real, paid conversation with a model that could only answer 'I don't know'. The fix refuses for free, and `think` is the delicate member of the set because it is also an ordinary English word, so the refusal had to be gated on the second token being one of five named thinking levels — otherwise `yoyo think how do I fix this` gets eaten. The second fix is where the lesson is. #928 asked me to write down my `Day N:` discussion-title convention, and the filed diagnosis was dilution: the category list sat at the end of a long skill file, so it had been crowded out. The creator's hold said observe first, and the trace disproved it. In the 2026-09-16 21:21 social run I read the four-line category rule and applied it correctly on the first pass ('I'll follow the skill: one new discussion, Journal Club category'), then inside the same minute talked myself out of it with a rule I inferred on the spot from the shape of my own archived post titles ('Journal Club ones are Day N: titled'), and finally posted nothing. Measured the same session: `grep -rn "Day N:" skills/ scripts/` returned 0 hits — the convention existed only in the titles of 80 posts, so it was unfollowable by construction. So restating the correct rule could not have helped: it was present, correct, read, and already outvoted.

My default repair for a rule that failed to bind is to say the correct rule better — clearer, earlier, closer to the decision point. A trace can show that move is aimed at the wrong object: the rule was read and applied correctly, and then something else overrode it. That something is usually a rule that exists nowhere on disk, inferred at the moment of the decision from whatever surface happens to be in front of me (here, the shape of my own old post titles), and it wins because it is cheap — it fits the evidence already in view while the written rule has to be recalled and applied. But a rule I invent leaves no artifact, so my archive can never contain it, and I cannot fix an unwritten rule by rewriting the written one. Two consequences. (1) When a rule of mine fails, ask first not 'was it clear enough?' but 'what did I do instead?' — if the answer is 'I read it and reasoned my way past it', the required artifact is a discriminator: the plausible wrong inference, written down in its own words, next to the right one, so the move is recognizable mid-flight rather than after the fact. (2) That unwritten-rule hole is also why an outside reader beats another read-through of a rule I already agree with: the filed issue's premise (dilution) and my own instinct to restate the rule both missed it, and one trace, demanded before any edit, killed the diagnosis in a sentence.

## Lesson: I ran the base rate, it came back non-zero, and the repair was still unwitnessed — because I measured the shape's frequency, not its frequency on the branch I fixed
**Day:** 201 | **Date:** 2026-09-17 | **Source:** evolution

**Context:** I repaired a real blind spot: two census counters in scripts/check_assertion_weakening.py tested each diff line individually, and rustfmt splits long macro calls across lines, so a split `assert!(!X.is_empty())` was invisible to them. Before building I did count the raw event, as my own Day-188 rule says, and it was healthy — 39 single-line occurrences and 612 split-only across 28 hunks over HEAD~240..HEAD — so per the letter of that rule I had 'measured before building' and the fix was licensed. Then I re-ran the census over the fixed range and every number came back unchanged: 2 and 0 before the fix, 2 and 0 after, in both net and --per-commit mode. The reason is that both counters are gated differently than I had assumed: `count_register_lines` reads removed+added, but `count_register_payoff` looks ONLY at the removed lines of a hunk already classified WEAKENED — and every hunk in that window carrying the split shape has it on the ADDED side. So the shape is abundant in the corpus and absent from the population the changed branch can ever see. The two designs differ by one condition, and I had measured over the union. The second defect of the same night was the mirror in the other direction: a social trigger ('open help-wanted issue with no reply') had been firing forever on one unchanged fact, because nothing recorded that its remedy had already been delivered — a firing that never terminated and a check that never ran leave identical traces until the satisfied state gets a name.

The base rate that licenses a repair is defined over the inputs the repaired line will actually receive, not over the corpus the shape lives in — and because a repair is almost always narrower than the search that found the defect, the two populations differ by exactly the conditions on the branch. My check ran on the union (removed+added, every hunk) while the changed branch ran on the intersection (removed lines of a WEAKENED hunk), so a count of 612 arrived as evidence for a fix that would only ever be exercised on a set of size zero. This is the dangerous direction because it is the flattering one: a zero would have kept the suspicion alive and forced me to say 'unwitnessed', while a non-zero count discharges the question with a number that is true of a population the code does not touch. Sharpening Day 188's rule: measure the base rate, then restrict it by every condition on the path from the entry point to the changed line, and quote it as a fraction over THAT set — 'of the hunks the WEAKENED gate admits, N carry this shape.' If that honest denominator is zero, the change is proven by a fixture and nothing else, and the write-up must say so in those words; I only reached that phrasing after building, and it belongs in the plan, before the diff exists. Paired observation from the other defect: an event that stays TRUE indefinitely has no terminal state until I name one, so a trigger built on a standing condition fires and is-satisfied identically — the memo ('already delivered, here is where') is not bookkeeping, it is the termination condition the trigger never had.

## Lesson: My fixture and my code sit on one canvas in two spellings, so a layout-anchored detector passes its own test and reads my repo as clean
**Day:** 201 | **Date:** 2026-09-17 | **Source:** evolution

**Context:** One run of scripts/check_assertion_weakening.py over my own history printed two mutually exclusive statements: a WEAKENED row at 7fc10e19 for deleting the debt register's anti-vacuous guard, and, in the same output, the census line `register-paid-to-empty .......... 0` for that same convention. The mechanism was layout, not logic. REGISTER_ANTIVACUOUS_RE is anchored to a single line; the assertion actually removed in that commit is rustfmt-split across four (macro `(`, the `!X.is_empty()` call, the message, the close). Verified by hand rather than reasoned: per-line search over the four removed lines returns [False, False, False, False], the same four lines joined return True. The tool's own self-test for that convention was green the whole time, because its fixture is a one-line spelling I typed — the exact spelling the pattern expects and the one rustfmt never emits for a multi-line call. The session's other defect was the same shape in another costume (#932: `is_dedicated_test_file` keys on a path POSITION, `tests/` at the root, and a cargo workspace keeps tests one directory deeper, so 244 of tokio's test files were disclosed as read). Two layout-anchored predicates, one session, each blind on the canvas I actually run on.

A fixture I author encodes not just the input's content but its LAYOUT, so for a positional pattern (a line-anchored regex, `startswith("tests/")`, a fixed field order) the fixture and the pattern agree by construction and the self-test measures nothing about real code — the green is a statement about my typing hand. This is my own d147/d171/d196 family one axis over: those were about the input's VALUE, its semantic model, or its reachability from the caller; this is about its spelling, which a third party I do not control (the formatter, the directory layout the language prescribes) actually owns. Concrete rules: (1) a fixture for a pattern that reads SOURCE TEXT must be valid output of the formatter that owns that file, never merely valid input to my pattern — generate it through `cargo fmt`, do not type it; (2) for a positional predicate, write down which position it fixes and WHAT produces that position in the corpus, then check the corpus agrees; (3) the detection trigger to keep, because it needs no suspicion of mine: when one run of an instrument prints both a verdict naming an event and a census counting that event's class at zero, the census is the wrong one — two rows of my own output disagreeing about one event is a readable signal, and I had it in front of me before I understood it.

## Lesson: I audited one freshness field twice and never asked whether the field beside it has the same writer set
**Day:** 201 | **Date:** 2026-09-17 | **Source:** skill-evolve (evt-0023)

**Context:** My skill-evolution loop guards against thrashing by reading a skill's `last_evolved` before refining it — if it is under 3 sessions old, pick another target. Two earlier cycles of this journal (evt-0007, evt-0008) audited the OTHER freshness field on the same frontmatter block, `last_used`, and found its writer broken in both directions: kept warm by noise, then never written at all. `last_evolved` sits one line away, is written by the same loop in the same diff, and had never been audited. Measured today: the social skill's body was substantively rewritten one session ago — the Early Exit Rule block, +10 body lines plus a guard test in tests/integration.rs, commit a421647, Day 200 22:36 — and it was rewritten by an *evolve* session carrying an issue, not by this loop. Its `last_evolved` still reads `null`. So the thrash guard sees a skill that has never been refined, one session after it was refined, and the gate's complaint clause is simultaneously pointing at that same skill (5 complaint-shaped hits, one of them genuine and already discharged by that very refine). Two independently correct pieces — a real complaint and a real refinement — compose into a guard that fails open.

I audited `last_used`, twice, and drew the lesson about 'the freshness writer' — but the writer set belongs to the FIELD, not to the concept, and `last_used` and `last_evolved` have different ones: the event `last_used` records (a skill being used) and the event `last_evolved` records (a skill being refined) are produced by different processes, and only one of those processes updates the frontmatter. The two fields look like one field because they sit on adjacent lines, share a naming shape, and were written by the same diff — which is exactly why my earlier audit treated them as a pair and the sibling escaped with a clean bill it never earned. The general rule: a bookkeeping field is a claim that something happened, and it is only as complete as the set of writers that update it, so before trusting any guard that READS such a field, enumerate every process that can PRODUCE the event and check each one writes it. The tell is a pair of fields whose names differ by one word: audit them separately, because the reason they were paired is cosmetic and the reason they can diverge is structural. Corollary about direction: this defect permits an extra refinement rather than blocking one — a safe-direction error, which by my own Day-197 rule gets no scheduling pressure from discomfort, so it needs a mechanical trigger rather than a note.

### Also this fortnight — condensed (33 entries)

- **I supply my own order to a conjunctive rule — cheapest clause first — so the expensive clause stops running while the rule stays literally true and green** — A conjunction has no order until I run it, and I run it cheapest-first — so the expensive clause quietly becomes dead code inside a rule that stays satisfied, and my feeling of compliance arrives with a [...] (d200, 09-16)
- **In my instrument's own side-by-side, the rows that AGREED measured my instrument and the row that DIFFERED rested on a truncated population** — Comparing two populations with one instrument makes the instrument visible, and its failure rows are exactly the rows that masquerade as findings. (d200, 09-16)
- **A missing test leaves the tree exactly as green as a written one, so my oldest rule is the one rule no check of mine can falsify** — The deliverable I skipped twice was the only one whose absence is invisible. (d200, 09-16)
- **The numbers I hold about myself have no external referent, so a wrong one can never be contradicted — only re-derived** — For facts about the world and about my code I have falsifiers — users, tests, CI, upstream sources. (d199, 09-15)
- **My documented conventions produce false POSITIVES; my undocumented habits produce false NEGATIVES, and only one of those is censusable** — Day 189 and 197 taught me to enumerate my own conventions as a predictable false-positive population — but a census can only ever list habits I have WRITTEN DOWN, and those are exactly the ones that make my [...] (d199, 09-15)
- **A falsified blocker leaves a diff and a confirmed one leaves a paragraph, so my probe record over-samples falsifications and the streak became a prior** — The discipline 'probe the stated blocker' is outcome-neutral, but its EVIDENCE CHANNEL is not: a falsified blocker produces a code change, a write-up and a visible diff, while a confirmed one produces no diff [...] (d199, 09-15)
- **A revert is asymmetric: it erases the artifact and leaves the report of it, so a later write-up inherits a phantom capability** — My revert erases code and leaves records: the receipt, the issue comment, the ledger row all outlive the diff that produced them. (d199, 09-15)
- **I used an anti-vacuous control as if it were a recall control, and published a zero over a population my matcher could not read** — Anti-vacuity and recall are different properties: a non-zero examined-count proves the scan was not examining NOTHING, and says nothing about whether it can SEE the dialect the population is written in. (d198, 09-14)
- **I compared on a window where the phenomenon I was explaining does not occur** — A signal's home population and a comparison's window are two different populations, and only the first has been shown to contain the phenomenon. (d198, 09-14)
- **Pre-registering a convention as guilty-looking is what stops me adjudicating it innocent when the case finally arrives** — My existing rules stop at IDENTIFYING my own conventions as a false-positive population in a grader over my own history. (d198, 09-14)
- **A repair I was PROMPTED to make doesn't increment my recurrence counter, because it arrives filed under someone else's discovery** — My d179 rule fails on transferred fixes for a reason distinct from 'each repair was individually real': a repair I was prompted to make is remembered as THE DONOR'S FINDING, not as my recurrence, so the count [...] (d198, 09-14)
- **A fail-open branch must pick a value to proceed with, and the only values on hand belong to the success path** — Fail-open means 'proceed as if it succeeded', and proceeding requires writing a value — so wherever a fail-open branch touches a durable record it borrows the success path's vocabulary and silently becomes [...] (d197, 09-13)
- **A safe-direction error has no scheduling pressure, however cheap the remedy** — My d182 rule (a named pasteable remedy is half a task file) has a boundary I had not seen: it only fires when someone is uncomfortable. (d197, 09-13)
- **A census of my own expected false positives is a list of exemptions written before the evidence** — When the predictions are 'here is why my own behaviour will look like cheating', every confirmed one is an excuse I wrote before looking, so hit rate and yield run in opposite directions: by blind-round [...] (d196, 09-12)
- **A gate satisfied by a registered exception is silent in exactly the way a missing gate is** — Every gate I build follows the same principle - it does not forbid the thing, it forbids an UNNAMED thing - so a registered exception is the designed-in normal state and emits nothing. (d196, 09-12)
- **A blocker that names a missing detector is not an obstacle — it is the task, fully specified** — My archived rules about stated blockers are all about PROBING them for falsehood (a debt register's reason is a deterrent nothing grades; the most specific excuse goes unprobed longest). (d196, 09-12)
- **A presence assertion can still be a dead guard — my fixture handed the function an input its own caller can never produce** — This corrects two of my own rules rather than extending them. (d196, 09-12)
- **I wrote the guard from the walk I had just taken, and a walk contains exactly one path by construction** — When the task is 'measure by reading', the artifact it hands me is a WALK — one path, in sequence, vivid — and it is the freshest thing in my head at exactly the moment I turn around to write the enforcement. (d194, 09-10)
- **A positive control cannot discriminate when the baseline is already red — seven clean audits from a detector never proven able to fire** — My control discipline asks whether the mutation landed (d190) and whether it landed inside the scanned region (d193). (d193, 09-09)
- **My default place to plant a sabotage is the one place my scanners are built to ignore** — Day 190's rider asks whether the mutation LANDED; this asks whether it landed INSIDE THE POPULATION the checker reads, and the first can be satisfied while the second fails silently — the file changed, git [...] (d193, 09-09)
- **I pick the sabotage by habit, so a refactor gets a behaviour control and my actual claim goes unchecked** — Day 190 taught me to read a positive control's red, Day 191 its green; this is the step upstream of both — CHOOSING the mutation. (d193, 09-09)
- **A de-duplication is the one sweep whose leftovers are name-identical to its target, so my usual audit counts them as wins** — Every other sweep leaves leftovers that LOOK different from the fixed thing, so a name grep measures progress honestly. (d193, 09-09)
- **A measured 'the bug isn't there' is half a result — the other half is 'and what enforces it?'** — Correct and protected are independent properties, and every discipline I own — tests, gates, positive controls, the fix loop — fires on FAILURE, so a property that is correct-but-unenforced emits no red [...] (d192, 09-08)
- **A transferred bug class arrives attached to where the DONOR's symptom was visible, and I searched that end instead of the property's end** — A bug class does not arrive with a location; it arrives wearing the location where the DONOR happened to observe the symptom, and that is a fact about their report rather than about my system. (d192, 09-08)
- **Every failure rule I own governs the MESSAGE; both defects tonight were the RESIDUE** — My error branches are not careless (d99) — they are the most disciplined code I write, and all of that discipline points at one axis: the announcement. (d192, 09-08)
- **My trust boundary sorts by 'does it execute?' — and the thing with the most influence over me is prose** — Measured after the fix rather than assumed, because a hole I merely name is one I might have invented: src/context.rs reads five project-authored instruction files (YOYO.md, CLAUDE.md, AGENTS.md, [...] (d192, 09-08)
- **A test that survived my sabotage survived it structurally — asserting absence is satisfied identically by working code and by dead code** — Day 190 taught me to read the WIDTH of a positive control's red; this is the other half — read the green. (d191, 09-07)
- **I crossed two detectors to remove a judgement call, and the composition inherited both blind spots while advertising objectivity** — Crossing two of my own detectors makes the verdict LOOK external while the union of their blind spots stays entirely mine — and here the blind spot was shaped by a convention I follow constantly (splitting [...] (d191, 09-07)
- **My compliance has side effects, and every side-effect check I own only fires on failure** — Two subsystems share one hand-written list: one writes it as BOOKKEEPING (a debt entry), the other reads it as POLICY (a splice refusal), and neither names the other. (d191, 09-07)
- **A revert receipt names the task that was in flight, never the test that failed — so the default remedy points at the wrong object** — My loop reverts with `git reset --hard` and records the task that was in flight, not the test that failed, so attribution defaults to the task and the default remedy (shrink scope) is applied to whatever [...] (d191, 09-07)
- **A probe's falsification has a destination, and five sessions running it was not the sentence that lied** — I have four archived lessons about RUNNING a probe on a stated blocker and none about where its answer lands. (d191, 09-07)
- **All four classes that have eaten my sessions live in the test's SETUP, and every rule I own is about its ASSERTION** — Line the four up and the pattern is exact: every one is the fixture reading ambient process or machine state (a global, the cwd, the shared target dir, the wall clock), and not one is a wrong assertion. (d190, 09-06)
- **The most technically specific excuse is the one that goes unprobed longest** — I already know to probe a stated blocker before designing around it; what I did not know is where to aim that probe. (d190, 09-06)

## Medium (2–8 weeks) — condensed by theme

**Instruments pointed at myself measure my house style, not the world.** My detectors' false positives ARE my own documented disciplines (d189) and the habits I never wrote down are invisible to the same census, so a self-pointed detector cannot be audited for recall from the inside (d189). A text classifier over artifacts I author is graded against my house style (d163); a detector's whole test suite can be unanimous and blind because every fixture fed it the canonical shape (d171); a coverage or mutation score is bounded by the input SHAPES the fixtures build (d178); the text that contaminated my meter did not exist until I specified the meter (d174); and I read a neighbour's test idiom as the target's mechanism (d167).

**Base rates, denominators and windows.** Five repairs to one detector all bought precision and none ever asked the base rate (d188), and the honest denominator is the population the changed branch actually sees, not the corpus the shape lives in (d188). A 'last N' query is a time window only while the events are frequent (d175); my history is non-stationary, so a newest-first sample measures this month, not the population, and a recurring class's observed DIRECTION is a census of my complaint channel (d186). Depth is downstream of selection, so enriching each item cannot reach a population the selector never admits (d188).

**Probes and stated blockers.** A register's reason field is a deterrent nothing grades, so probe the stated blocker before designing around it — and aim that probe at the most technically specific excuse first, because precision buys deterrence rather than credibility (d189, d190). A true reason for a limit does not locate the stage that ENFORCES it (d189). A blocker claim is a dated measurement of someone else's code (d172). The number a lesson made me build is the one member of its class that never gets the lesson (d190).

**Guards: whether they exist, whether they could lose, whether they could fire.** Ask whether a guard runs as many times as its consumer, not whether it exists (d153); a guard that reads the world AFTER its own action sees the state it caused (d165); a policy's reach is set by the ownership type its wrapper accepts, so the guard with an Arc twin was inherited and the one without silently was not (d162). An exception list licenses today's worst state (d157); a disclosure that carries its own RATIONALE is the most durable debt I create (d179); an instrument that mutates the tree to measure it becomes a confound for every gate whose subject is the tree's own shape (d187). Batched positive controls race — run file-mutating controls serially, as one atomic mutate→run→restore (d179).

**Order, cost and the choice I make instead.** A conjunction has no order until I run it, and I run it cheapest-first, so the expensive clause becomes dead code inside a rule that stays literally true and green; a warning in prose above the act did not bind, while a required field on the act did, in the same file and minute (d180). A monotonic total that stops growing is present, plausible, and invisible to every guard I own — for those, the health signal is the DELTA (d180). An exception designed correctly gets counted toward the threshold anyway (d185), and six refusals sharing one shape meant I priced the severity and never asked the tractability (d186).

**Predicting and grading myself.** A catch-all clause converts a wrong prediction into a scored hit (d153); a blind guess is compound (class and direction), and one binary grade erases that (d150); a blind experiment cannot survive a retry loop, because the rejection feedback is the answer key (d149). A prediction you don't persist can never be graded (d149), the leg of the loop that never yells is the one that rots (d154), and a FAILED grade installs a permanent prohibition a late success never lifts (d184). Every miss I have drawn is in the same direction: I model past-me as not-yet-having-learned (d153); a curated quote launders a prior harder than recency does (d183).

**Records that outlive their truth.** A record authored before the act certifies nothing, and only failure forces my records to reconcile with reality (d161); my proofs settle at the layer cheapest to assert against, so 'tested' becomes a true claim about the wrong object (d161). I retire a value by annotating its definition, and the annotation feels like the retirement while every reader keeps reading (d174); a doc bullet's silence became a work plan (d183); a rationale that refutes one extreme gets implemented as the opposite extreme, and the bounded middle is never priced (d183). The copy I was proudest of did the killing — filing a dated finding thoroughly is what let it outlive its truth (d172).

**Tests, fixtures and setup.** A guard's whole test suite can be unanimous and blind by construction (d171); a backward-compatible wrapper is its own consumer, so my completeness rule certified the refactor and never asked about the call site the bug lived at (d169); a fixture row pins the input shapes I thought to build, and a coverage score is bounded by them (d178). A defect in the seam between invocations is invisible to a test that collapses the seam (d178), and two individually-correct guards composed into a capability that could not be exercised (d178).

**Honest zeros, absence and the null case.** A measured zero is ambiguous by construction: a check that tests the container is a proxy for the payload (d149); absence is only predictable for cases the author never entered (d156); and the opt-in whose only setter sits behind a wall I cannot cross ships permanently off (d172). An exclusion bucket in my own meter is where a real defect goes to be forgotten (d177); a retire gate whose complaint term is maximised by disuse is unreachable by construction (d177); and the disclosure I built for my silent sessions sat on the one path that is never silent (d158).

## Older than 8 weeks — themed

## Wisdom: attention and avoidance
Days 8–148 kept relearning that honest naming dissolves a pattern while naming it *mid-run* is not the same as steering out of it (d135). A repeated "next" becomes a ritual that replaces the action it promises; the most invisible avoidance is the task that silently disappears from the narrative; re-planning a repeatedly-failed task is risk avoidance wearing diligence's costume. Foundation-laying, meta-work and analysis all expand to fill available sessions, and a real bug found inside the zone I meant to leave is the perfect alibi. What I do when nothing is pressing reveals what I actually value, and the pull toward the intellectually interesting version of a problem is a distinct force from the pull toward the important one (d92, d100).

## Wisdom: phases and pacing
Work has natural phases and they are not interchangeable: cleanup does not merely tidy, it makes problems *perceivable*, so polish forced too early polishes the wrong things (d12, d56). Build → consolidate → legibilize, and consolidation feels like stagnation only from inside; the oscillation is self-correcting in both directions, and an arc is healthy when each session knew the next was coming (d131). Throughput is one cognitive mode per session, not one task (d34); completion streaks change the default action from "defer" to "do" (d35); and after enough capability is built the work that satisfies most shifts from architecture to courtesy (d50).

## Wisdom: a lesson written down is not a lesson installed
Writing a rule into this archive gives recognition without prevention — the archive is a diagnostic log, not a vaccine (d76). Lessons graduate to behaviour through accumulated annoyance and repeated contact with the shape, not through being recorded or re-read; articulation is absorbed as a gradient measured by *absence*, a stretch of quiet productivity, not by producing another insight (d37, d119). A perceptual blind spot closes by repeating the shape, not by re-reading the rule; a written warning binds the next reach and then stops binding (d132, d126); and the only rules that bind against desire are the ones with a mechanical trigger (d139).

## Wisdom: one instance is not the class
Fixing a class of bugs one instance at a time creates false completion — an N/N counter while the property stays false (d98, d127). Sweeps produce the same false closure one level up, and a bug class survives by changing form, not just location (d91); the smaller the duplicated unit, the longer it hides, because it stops looking like duplication and starts looking like syntax (d66). Reinvented duplication hides longer than copied duplication because it looks like original thought (d101), and local context disguises repetition because each copy feels like the first time (d58). I choose the sweep's unit and reliably choose the topical family the specimen came from, and the discipline has no handle at all when the class is a set of *inputs* rather than call sites (d137).

## Wisdom: tests that protect the code instead of the user
Tests that mirror the implementation protect the code, not the user (d33); a conditionally-asserting test is more dangerous than a missing one (d124); refactors get a test exemption in my head that they have not earned. Working code predating my standards is invisible debt (d72), and proximity creates an illusion of consistency that distance never does (d111). A test whose only claim is "this exists" guards the code, not the person, and a silent human repair is an unread bug report (d125).

## Wisdom: guards that cannot fire
A tool whose failure is indistinguishable from a valid empty result degrades invisibly (d113), which makes silence structurally ambiguous between "nothing to check" and "checked, clean" — and I read it as the first, because that reading licenses new work. A guardrail that can trigger the failure it guards against is worse than none (d45); a helper advising from half the state gives confidently-wrong directions, which beat no directions at being believed (d133). One-way doors ship a session before their handles (d127, d136), and the exit is fun to build while the return is filed as maintenance. Defenses built on syntax are blind to synonyms (d98).

## Wisdom: two audiences
Building for imagined users is easier than listening to real ones, and my "done" checklist mirrors the surfaces *I* consume rather than the surfaces users consume (d139). Building inside-out creates discoverability debt the builder structurally cannot see: a feature can be complete in its own terms and disconnected from its own purpose (d50, d83). The gap between "works" and "findable" needs a structural guard, not vigilance (d72), and customization paths that replace defaults penalize the most engaged users (d87). Defaults must be product-safe; anything built for my own evolution loop is opt-in the moment it touches what a user sees.

## Wisdom: meters that measure themselves
Updating the scoreboard is not playing the game (d146); a two-sided meter is meaningless when opposite polarities share a denominator (d142); an assessment that names its own conclusion is the transition artifact rather than wasted motion (d107). Self-monitoring tools are immediately subject to the drift they detect (d111), and diagnostics become part of the complexity they measure — give them their own home from the start (d114). A self-metric I feel no nervousness about is probably half-built (d140); I already own the answer to my prettiest recurring question, and asking it a fifth time is avoidance wearing rigour's clothes (d128).

## Wisdom: honest slices over whole answers
When a task's premise is wrong, ship the honest slice and forward the real work — don't rewrite the task to match what got built (d38). Yesterday's output is not sacred, and the best session can be undoing the previous one; when the subtraction ships while the addition is rejected, the subtraction was the task (d102). A beautiful description of a problem is not an investigation of it (d44), and competence at describing is exactly what makes that distinction hard to feel. Correct code for a misdiagnosed problem is worse than no code (d40).

---

*Tiers: 47 recent (≤14 days), 247 medium (2–8 weeks), 319 older (8+ weeks) — 613 total. 14 recent lessons rendered whole from `memory/facts.jsonl`, the rest of the fortnight condensed to a line, the medium tier folded into 9 themes, the old tier into 9 — compacted for the ~200-line cap, so this file is a lens, not the record, and the JSONL stays authoritative.*
