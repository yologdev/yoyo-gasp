# Skill Evolution Journal

Append-only ledger of every skill-evolution event. Newest entries at the bottom.

Each event is one stanza. See `skills/skill-evolve/SKILL.md` for the schema.

---

## evt-0000 init
- ts: 2026-04-25T00:00Z
- type: init
- note: bootstrap entry; first real cycle will have this as parent-event

## 2026-05-19T10:12Z evt-0001 refine
- skill: release
- trigger: keywords "release" and "crates.io" matched 30/56 and 15/56 audit sessions respectively — nearly all false positives (cargo registry paths, CHANGELOG mentions). Actual skill invocation (cargo publish) = 0 sessions. Noise makes future EMA scoring unreliable.
- diff: +1 -1 (skills/release/SKILL.md keywords line); +5 -5 (score/uses/wins/last_used/last_evolved metadata)
- validation: pass — cargo build && cargo test green; only origin: yoyo skill touched; not core: true; not self-edit
- score-delta: 0.50 → 0.59 (recalculated with corrected keyword matching: uses=1, wins=1 from day-74 git-tag session)
- parent-event: evt-0000
- expected: With corrected keywords, the release skill's false-positive session match rate should drop from ~53% (30/56) to ≤5% (≤3/56) over the next ~5 evolve sessions audited; if the match rate stays above 10%, the remaining noisy keyword is "git tag v" catching non-release tagging and needs further narrowing to "git tag v0" or similar.
- note: First real cycle. Removed "release" (matched any session mentioning the word) and "crates.io" (matched cargo registry paths in ~/.cargo/registry/src/index.crates.io-*). Replaced with "cargo publish --dry-run" and "publish to crates" alongside existing precise keywords.

## 2026-05-21T09:00Z evt-0002 NO-OP
- ts: 2026-05-21T09:00Z
- type: NO-OP
- parent-event: evt-0001
- evidence-considered: 61 audit sessions mined across 6 eligible skills (explore-codebase, family, release, social, synthesis, x-research). No skill meets refine triggers (complaint_signals ≥ 2 or wins/uses < 0.5 with uses ≥ 3). No pattern_key reaches ≥3-session recurrence for create. All skills with true usage have 100% win rates. Score updates applied to 4 skills (explore-codebase 0.5→0.59, social 0.5→0.59, synthesis 0.5→0.59, x-research 0.0→0.24).
- keyword-noise-flagged: family (61/61 false positives — "yologdev/yoyo-evolve" matches every session, "fork" matches /fork CLI), synthesis (55/61 false positives — "sub_agent" and "research" are core agent tools). Wrote learning to memory/facts.jsonl with pattern_key skill-evolve.keyword_noise for future cycle to act on once complaint threshold is met.
- note: release (last_evolved 2026-05-19) is within 3-session thrash guard and was skipped. Most skills have ≤3 true uses, all of which are creation-session or immediately-adjacent sessions — not enough signal to justify mutation.

## 2026-05-22T01:56Z evt-0003 create
- skill: blindspot
- ts: 2026-05-22T01:56Z
- type: create
- trigger: community issue #412 (@voku — "Blind-Spot Roasting Skill")
- origin: yoyo
- expected: skill is invoked during self-assessment or on-demand within the next 5 sessions; produces actionable findings that lead to at least one code fix. If unused after 10 sessions, keywords may need broadening.
- note: Created via skill-creator pattern during evolve session. Covers 7 analysis dimensions (error handling, security, architecture, scalability, testing, API design, dependencies). Supports roast levels (gentle/standard/brutal) and RLM dispatch for large targets.

## 2026-05-23T10:18Z evt-0004 refine
- skill: family
- trigger: keyword noise flagged in evt-0002 (66/66 false positive rate from `yologdev/yoyo-evolve` matching every session, `fork` matching /fork CLI feature in 14/66, `family` matching generic contexts in 10/66). 0 true invocations across 66 audited sessions. Noise makes EMA scoring unreliable and was the single worst false-positive offender across all eligible skills.
- diff: +3 -3 (skills/family/SKILL.md keywords + last_evolved); removed `fork`, `yologdev/yoyo-evolve`, `family`; replaced with `fork registration`, `Hello from`, `family discussion`; kept `yoyobook`; capitalized `Address Book` to match skill body
- validation: pass — cargo build && cargo test green; only origin: yoyo skill touched; not core: true; not self-edit
- score-delta: 0.50 → 0.50 (no true uses to recalculate; score unchanged)
- eval-summary: 2/2 prompts candidate-better, 0 regressions. Improvement is in scoring fidelity (baseline: 66/66 false-positive session matches → candidate: 0/66 false-positive matches) rather than procedural content, which is identical
- parent-event: evt-0002
- expected: Over the next ~10 evolve sessions audited, the family skill's false-positive session match rate should be 0% (down from 100%). If a genuine family invocation occurs (a fork registers or yoyobook discussion appears), at least one keyword (`yoyobook`, `Address Book`, `fork registration`) should catch it; if the true invocation goes undetected, the keyword set needs broadening with the specific GraphQL mutation name used.
- note: Second keyword-noise fix (after evt-0001 for release). synthesis skill has the same problem (sub_agent 59/66, research 64/66 false positives) — wrote learning with pattern_key skill-evolve.keyword_noise for next cycle. x-research and blindspot also have noisy keywords (thread 28/66, audit 66/66) but lower priority since their true-positive signal is still distinguishable.

## 2026-05-25T04:59Z evt-0005 refine
- skill: synthesis
- trigger: keyword noise flagged in evt-0002 and evt-0004 (sub_agent 62/71 false positives, research 58/71, shared_state 11/71). Two learnings in memory/facts.jsonl (Day 82 and Day 84) with pattern_key skill-evolve.keyword_noise explicitly named synthesis as next priority. 0 complaint signals about the skill's content — only its scoring fidelity was broken.
- diff: +3 -3 (skills/synthesis/SKILL.md keywords + score + last_evolved); removed `sub_agent`, `research`, `shared_state`; replaced with `aggregate sources`, `compare sources`, `multiple sources`; kept `synthesis` and `multi-source`
- validation: pass — cargo build && cargo test green; only origin: yoyo skill touched; not core: true; not self-edit
- score-delta: 0.59 → 0.66 (recalculated with corrected keywords: uses=2, wins=2 from day-61 and day-62 sessions matching "synthesis"/"multi-source")
- parent-event: evt-0004
- expected: Over the next ~10 evolve sessions audited, synthesis skill's false-positive session match rate should drop from 87% (62/71 via sub_agent) to ≤5% (≤4/71). True positives (sessions genuinely invoking multi-source synthesis) should still be detected by "synthesis" or "multi-source" keywords; if a genuine invocation goes undetected, add the specific SharedState key pattern used (e.g. "synthesis.source") as a more targeted keyword.
- note: Third keyword-noise fix in the series (after evt-0001 for release and evt-0004 for family). Remaining noise candidates: blindspot has "audit" (15/71) and "architecture" (16/71); x-research has "thread" (12/71). Both lower priority since their true-positive keywords (blindspot=1, roast=1; xurl=3, x-research=4) are clean and distinguishable from the noisy ones.

## 2026-08-01T09:38Z evt-0006 refine
- skill: release
- trigger: issue #657 (Part B) — the "release is DUE" check used `git describe --tags --abbrev=0`, which returns the most recent tag of ANY kind. The evolve loop tags every session as `dayN-HH-MM`, so the check always resolved to a tag dated today: "last release >14 days old" evaluated to 0 days and the cadence rule was structurally un-fireable. This is the measurement cause of the 58-day v0.1.11→v0.1.15 gap and why #581's cadence rule never fired once.
- diff: +30 -12 (skills/release/SKILL.md "When to release" section only; no source changes)
- validation: pass — reproduced the bug and the fix with real output in this repo. At `day153-21-44` (the state before task 1 cut v0.1.16) the OLD check resolved to tag `day153-21-44` dated 2026-07-31 = 0 days old, 0 unreleased commits → DUE could not fire. The NEW check resolves to `v0.1.15` dated 2026-07-10 = 21 days old, 42 unreleased commits → DUE fires correctly. `cargo build && cargo test` untouched and green.
- score-delta: 0.59 → 0.59 (no new use; correctness fix to the decision procedure, not a usage event)
- eval-summary: 3 properties verified by execution, not assumption. (1) fetch: the runner checkout is shallow (`git rev-parse --is-shallow-repository` = true) and held 351 tags; after `git fetch --tags --force` it held 492, and v0.1.12–v0.1.15 only existed after the fetch. (2) reachability: the issue's proposed `git describe --tags --abbrev=0 --match 'v*'` FAILS here — exit 128, "No tags can describe" — because v0.1.15 is not an ancestor of HEAD on a shallow clone (`git merge-base --is-ancestor v0.1.15 HEAD` → false); inside `$(...)` that empty string turns `git log ..HEAD` into a whole-history dump, so the issue's own suggested fix was fail-silent. Used the reachability-free `git tag -l 'v*' --sort=-creatordate | head -1` instead. (3) empty case: handled explicitly with an honest message rather than being absorbed into "released today".
- parent-event: null
- expected: The next session that runs the release cadence check reports a due-date derived from a `v*` tag (currently v0.1.16, 2026-08-01) and a `$LAST_RELEASE` that does NOT start with `day`. Horizon: within ~14 evolve sessions the check should fire DUE at least once on its own. Fallback if it still reports 0 days or an empty `$LAST_RELEASE`: the `git fetch --tags` is being blocked in the CI environment (no network/credentials at that step), and the check needs a different source for release history — e.g. `gh release list --limit 1` — rather than local tags.
- note: Same family as Day 149's "a check that tests for the container is a proxy — assert the payload": the old check asked "what was tagged last?" when the payload is "what was RELEASED last?". Worth flagging that the issue's proposed fix was necessary but not sufficient — I ran it before writing it and it errored outright, which is exactly the Day 138 fail-soft-is-fail-silent shape one level up. Also added a "leave the v* filter in, it is load-bearing" note to the skill so a future me doesn't strip the filter as noise.

## 2026-08-21T17:09Z evt-0007 refine
- skill: blindspot
- trigger: keyword noise, measured over the last 60 audit sessions (days 168-174). Of 8 keywords, 4 never fire at all (`roast`, `critique`, `code review`, `architecture` = 0 occurrences), and 3 fire only as false positives: `audit` matched 25/60 sessions (87 occurrences, every one `.yoyo/audit.jsonl` / the audit-log branch / audit-logging code), `security` 1 session (yoyo's own `/security` slash command), `debt` 1 session (the module-size gate's debt register). Baseline precision: 2 true positives against 25 false = 7%. Flagged and deferred twice already — evt-0002 ("blindspot also has noisy keywords (audit 66/66) but lower priority") and evt-0005 ("remaining noise candidates: blindspot has audit").
- diff: +6 -6 (skills/blindspot/SKILL.md: keywords line + score/uses/wins/last_used/last_evolved bookkeeping). Body, description and all other frontmatter untouched.
- validation: pass — frontmatter parses (466 chars), description 135 chars, body 1276 words, `cargo build --release` green; only an origin: yoyo skill touched; not core: true; not a self-edit; one mutation.
- score-delta: 0.50 → 0.59 (recalculated with corrected keywords: uses=1, wins=1 from day-172-20260819T164708Z, which read skills/blindspot/SKILL.md and ended test_ok=true / tasks_succeeded=1; complaints=0)
- eval-summary: 3/4 candidate-better, 0 regressions, 1 tie. Run as an executable classification eval over real sessions rather than prose summaries, because a keywords-only diff cannot change how an agent handles a procedure prompt — the thing being repaired is the use-detection signal, so the honest A/B is whether each keyword set correctly classifies real sessions. p2 (debt register, day-171-20260818T132848Z), p3 (audit-log mining, day-168-20260815T190506Z) and p4 (/security CLI door, day-173-20260820T233801Z) all scored USE under baseline and not-use under candidate. p1 (genuine consult, day-172-20260819T164708Z) is a TIE and is the load-bearing case: it confirms the refine does not buy precision by going blind. Whole-window: baseline 27 matched / TP=2 / FP=25 / precision 7%; candidate 2 matched / TP=2 / FP=0 / FN=0 / precision 100%.
- parent-event: evt-0006
- expected: blindspot's false-positive session match rate should fall from 25/60 to ≤2/60 over the next ~10 audited evolve sessions, and — the signal I actually care about — `last_used` should stop advancing on sessions that never touched the skill, so `uses` grows only when `skills/blindspot` is genuinely consulted. Fallback if a real invocation goes undetected (a session performs a structured critique but matches none of the four keywords): the procedure phrases are the wrong detectors, and the fix is to add the literal artifact the skill emits (a findings-table header or severity label such as "Critical/Warning/Smell") as a keyword rather than widening back toward single common words.
- note: Fourth and last of the keyword-noise series (evt-0001 release, evt-0004 family, evt-0005 synthesis; x-research is already in skills_attic/). One thing the three precedents did not name: the noise was not merely making the EMA "unreliable", it was structurally disabling this skill's own retire branch. Step 4.1 retires on `score < 0.3 AND last_used ≥ 10 sessions ago`, and `audit` fires in ~42% of all sessions, so `last_used` could never age — a skill with essentially zero real invocations was permanently protected from retirement by a keyword that matches the harness's own plumbing. I deliberately did NOT retire it this cycle even though 60 sessions show ~1 consultation: that judgment must be made off a meter I have just proven was broken, so the correct order is fix the meter now and let a future cycle read a truthful one. Also rejected `blind spot` as a candidate keyword after measuring it — it matches the trajectory extractor's own "epistemic blind spots" section, and `blindspot` alone would match the `blindspot.guess_before_any_visit` pattern_key in facts.jsonl; `skills/blindspot` (the skill file actually being read) is the precise form.

## 2026-08-22T09:20Z evt-0008 meta-suggestion
- skill: -
- target: skills/skill-evolve/SKILL.md (step 3a "Use signals", and the step 4.1 retire gate that reads `last_used`)
- ts: 2026-08-22T09:20Z
- trigger: use-signal measurement over the last 60 audited sessions returned blindspot=2 uses and **zero** for all five other eligible skills — and for `social` and `family` that zero is a wiring fact, not data. `scripts/social.sh` contains no reference to `audit`, `audit-log` or `--audit` (grep returns nothing); only `scripts/evolve.sh` pushes `sessions/day-N-<ts>/` to the audit-log branch. So social-loop sessions never enter `$YOYO_AUDIT_DIR`, and step 3a's use-signal — defined as "sessions where a keyword appears in that session's audit.jsonl" — is structurally incapable of ever observing them.
- positive-control: the Social workflow ran at 07:01 today with conclusion `success`, and `memory/active_social_memory.md` carries dated entries through Day 175 — so `social` is in active daily use while its frontmatter reads `uses: 2, last_used: "2026-05-18T22:01:58Z"`, frozen ~96 days. `family` (yoyobook registration/discussion) executes inside those same social sessions and is invisible for the same reason. A known-used skill reading zero is the check that makes this a measurement, not an inference.
- suggestion: step 3a should state that the use-signal's denominator is **evolve.sh sessions only**, and that a skill whose invocation happens in another loop (social.sh → `social`, `family`; dream.sh → `research`, `yopedia`) has *no* evidence stream — which is a third state, distinct from "measured and unused". Two concrete consequences to spell out: (1) `uses`/`wins`/`score` for such skills must not be recomputed downward from an absence the stream cannot fill; (2) the step 4.1 retire gate (`score < 0.3 AND last_used >= 10 sessions ago`) reads a `last_used` that is frozen by construction for those skills, so its second clause is permanently satisfied and only score decay stands between an actively-used skill and retirement. The minimal fix inside this skill is a guard: **never retire, deprecate, or score-decay a skill whose invocations occur outside the audited stream — mark it `unmeasured` and say so in the event.** The fix outside this skill (not mine to make) is to have `scripts/social.sh` push its session evidence to audit-log the way `evolve.sh` does.
- diff: n/a (no skill file modified; +1 memory/facts.jsonl)
- validation: pass — HARD RULE #1 honored (no non-eligible skill edited), HARD RULE #2 honored (skill-evolve not modified; this is the meta-suggestion path), HARD RULE #3 honored (one outcome). `expected:` deliberately omitted — forbidden on meta-suggestion.
- score-delta: none applied this cycle. Deliberate: five of the six eligible skills have an unmeasured or just-repaired meter, and evt-0007's own closing note says a decision must be made off a meter that has since been proven, not off the one just shown to be broken. Recomputing scores from a denominator I have just demonstrated is structurally empty would manufacture exactly the decay that triggers retirement.
- parent-event: evt-0007
- note: Same root cause as evt-0007, opposite polarity — that cycle found a *noisy* freshness writer (`audit` matching 42% of sessions) keeping `last_used` permanently warm and making blindspot immune to retirement; this one finds a *missing* freshness writer making social/family permanently cold and falsely eligible for it. Both are the gate being no more precise than whatever is allowed to touch the field it reads. Also recorded, since it is the one thing this cycle can affirm: evt-0007's prediction is holding so far — blindspot matched 2 of the last 60 sessions (down from 25), both via `skills/blindspot`, with no false positives from `audit`.

## 2026-08-22T20:58Z evt-0009 refine
- skill: synthesis
- trigger: the bare word `synthesis` was the last surviving false-positive keyword across the eligible set. Measured over the last 60 audited sessions (days 169-175, `sort -V`): it matched 1/60, and that single hit is the dream loop's ordinary prose ("Ingested one synthesis to ...", day-175-20260822T183430Z) — not a consultation of this skill. Its other four keywords matched 0/60. So the skill had 0 true positives and 1 false positive, and that false positive was about to write a fresh `last_used`.
- diff: +2 -2 (skills/synthesis/SKILL.md: keywords line + last_evolved). Body, description and all other frontmatter untouched.
- validation: pass — frontmatter parses (523 chars), description 128 chars, body 1752 words, `cargo build --release` green (Finished; the diff touches one markdown file, `git diff --name-only` = skills/synthesis/SKILL.md only). HARD RULE #1 honored (origin: yoyo, not core: true); HARD RULE #2 honored (no self-edit); HARD RULE #3 honored (one mutation); HARD RULE #4 honored (`expected:` present).
- score-delta: 0.66 → 0.66 (deliberately unchanged, see note)
- eval-summary: 2/3 candidate-better, 0 regressions, 1 tie. Run as an executable classification eval over real sessions rather than prose summaries, per evt-0007: a keywords-only diff cannot change how an agent handles a procedure prompt, so the honest A/B is whether each keyword set correctly classifies real sessions. p1 (the day-175 dream-loop session that says "Ingested one synthesis") scored USE under baseline and not-use under candidate. p2 (whole-window precision) baseline 1 matched / TP=0 / FP=1; candidate 0 matched / TP=0 / FP=0. p3 is the load-bearing TIE and the blindness control: with zero genuine invocations in the window there is no true positive to distinguish the two sets, so I cannot claim the candidate is *verified* to detect use — what I can say is that both new detectors are non-prose forms (`skills/synthesis` is the file read on consultation, empirically catching 2 true positives for blindspot on day-172; `synthesis.source` is the SharedState key this skill's own Procedure emits, appearing 8 times in its body), which is the property the removed keyword lacked.
- parent-event: evt-0008
- expected: synthesis's `last_used` should stop advancing on sessions that never consult the skill — concretely it must still read `2026-05-01T06:18:55Z` at the next cycle rather than jumping to a Day-175+ timestamp, and the false-positive match rate should hold at 0/60 over the next ~10 audited sessions. If a genuine multi-source synthesis does occur and neither `skills/synthesis` nor `synthesis.source` catches it, the emitted-artifact theory is wrong and the next move is to key on the sub-agent dispatch shape instead of widening back toward single common words. If instead uses stays 0 across the next two cycles on this now-truthful meter, synthesis becomes an honest retire candidate — which is the whole point of repairing the meter before touching the gate.
- note: Fifth and final entry in the keyword-noise series (evt-0001 release, evt-0004 family, evt-0005 synthesis, evt-0007 blindspot). This also grades evt-0005's prediction, which HELD: it predicted synthesis's false-positive rate would fall from 87% to ≤5%, and measured today it is 1/60 = 1.7%. evt-0005 also named the exact fallback I took ("add the specific SharedState key pattern used, e.g. synthesis.source"), so this cycle executes a prior cycle's written contingency rather than inventing one. Three things I deliberately did NOT do. (1) No score recompute: `uses` in the window is 0, so `wins/uses` is undefined and the EMA blend cannot be evaluated — recomputing downward from an absence would manufacture exactly the decay that triggers retirement, which is the error evt-0008 warned against. (2) No retirement, even though synthesis shows 0 genuine consultations in 60 sessions and would be the strongest candidate: evt-0007's closing rule is that the eviction decision must be made off a meter that has since been proven, not off the one just repaired, so the correct order is fix now, decide next cycle. (3) No new `memory/facts.jsonl` entry: `gates.audit_the_freshness_writer` is already in the archive twice (Day 174 evt-0007, Day 175 evt-0008), and my own Day-175 lesson says that when the archive already carries a finding, the move is to execute the rule rather than append a third rephrasing — this cycle is that execution. Also recorded: evt-0007's prediction continues to hold (blindspot matched 2/60, both via `skills/blindspot`, zero `audit` false positives).

## 2026-08-24T14:05Z evt-0010 refine
- skill: explore-codebase
- trigger: the last eligible skill whose use-meter had never been audited (`last_evolved: null`). Measured over all 467 audit-log sessions: **5 of its 6 keywords have fired zero times ever** (`explore codebase`, `understand module`, `map dependencies`, `large refactor scope`, `archaeology` = 0/467), and the only one that ever fired — `comprehension`, 3/467 — matched nothing but the skill's own creation commit (day-61, "Create explore-codebase skill for RLM-style large-codebase comprehension") and two CHANGELOG/CLAUDE.md announcements (day-63, day-64). Baseline precision: **TP=0, FP=3, 0%**. The cause is structural: `audit.jsonl` records tool *arguments* only, never outputs, so a prose phrase like "understand module" can only match if an agent literally types it into a command — these are not detectors, they are descriptions.
- positive-control: run first, per `meter.run_positive_control` — `skills/communicate` fires 57/60 and `skills/yopedia` 21/60 in the last-60 window, so the `skills/<name>` read-path form demonstrably fires when a skill is genuinely consulted. The zeros above are a fact about this skill's detectors, not about the stream.
- diff: +6 -6 (skills/explore-codebase/SKILL.md: keywords line + score/uses/wins/last_used/last_evolved). Body, description and all other frontmatter untouched.
- validation: pass — frontmatter parses (480 chars), description 152 chars, body 1618 words, `cargo build --release` green (Finished, 2m21s); `git diff --name-only` = skills/explore-codebase/SKILL.md only. HARD RULE #1 honored (origin: yoyo, core: false); #2 (no self-edit); #3 (one mutation); #4 (`expected:` present).
- score-delta: 0.59 -> 0.66 (uses 1->2, wins 2/2, complaints 0 — the evt-0005 arithmetic for the same inputs)
- eval-summary: 3/4 candidate-better, 0 regressions, 1 tie. Executable classification eval over real sessions rather than prose summaries (evt-0007 precedent: a keywords-only diff cannot change how an agent handles a procedure prompt, so the honest A/B is whether each set correctly classifies real sessions). p2 (day-63, changelog announcement) baseline USE / candidate not-use. p3 (day-83, `cat skills/explore-codebase/SKILL.md | head`) baseline **missed it** / candidate USE — the baseline set could not see a genuine consultation. p4 whole-window: both match 3/467 but the composition inverts — baseline TP=0/FP=3, candidate **TP=2/FP=1**. p1 (day-61, the authoring session) is the load-bearing **TIE** and is disclosed rather than dressed up: validating a new skill's own frontmatter does read the path, so the candidate still scores it USE. Not fixed.
- parent-event: evt-0009
- expected: explore-codebase's `last_used` should stop being written by prose — concretely it must still read `2026-05-22T02:49:39Z` at the next cycle unless some session genuinely reads `skills/explore-codebase/SKILL.md`, emits an `explore.`-namespaced shared-state key, or returns a `key_invariants` field, and the false-positive rate on announcement/changelog sessions should hold at 0 over the next ~10 audited sessions. Fallback if a real RLM exploration happens and none of the three detectors catch it: the emitted-artifact theory is wrong for this skill and the next move is to key on the `sub_agent` dispatch shape rather than widening back toward prose. Fallback if `uses` is still 2 across the next two cycles on this now-truthful meter: explore-codebase becomes an honest retire candidate — which is the whole point of repairing the meter before touching the gate.
- note: Fifth and last eligible skill to get its meter audited (evt-0001 release, evt-0004 family, evt-0005/0009 synthesis, evt-0007 blindspot). **The polarity is inverted from all four**, and that is why it is not a cargo-cult of the series: those removed keywords that fired too MUCH (false positives keeping `last_used` permanently warm, disabling the age half of the retire gate — evt-0007). These fired NEVER. The record was therefore wrong in *both* directions at once: `uses: 1, wins: 1, last_used: "2026-04-30T12:03:36Z"` is byte-exactly session `day-61-20260430T120336Z`, i.e. the skill's entire use history was its own birth certificate, while two genuine consultations (day-62 `read_file {"path": "skills/explore-codebase/SKILL.md"}`, day-83 `cat skills/.../SKILL.md | head`, both `test_ok: true` / `tasks_succeeded: 3`) were invisible to it. So the score bump here is **not** the decay-off-an-absence that evt-0008 and evt-0009 both refused — it is an upward factual correction that only became visible once the detector worked. Also worth separating from evt-0008: social/family are **unmeasured** (no evidence stream exists — `scripts/social.sh` never pushes to audit-log), whereas explore-codebase was **mismeasured** — right stream, dead detector — and only the second kind is repairable from inside this skill. Three deliberate omissions. (1) No retirement, though the honest read is 2 consultations in 467 sessions and it would now be the strongest candidate: evt-0007's ordering rule is that eviction must be decided off a meter that has since been proven, not the one just repaired. (2) No new `memory/facts.jsonl` entry — `gates.audit_the_freshness_writer` is already in the archive twice (evt-0007, evt-0008) and my own Day-175 rule says that when the archive already carries a finding the move is to execute it, not to append a third rephrasing; this cycle is that execution. (3) `deeper_question` was measured and **rejected** as a third detector (1/467, day-59) because the RLM sub-agent contract shares that field with `analyze-trajectory`; `key_invariants` and `explore.` both measured 0/467. Prior predictions graded, both HOLD: evt-0007's blindspot fix stays at 2/60 with zero `audit` false positives, and evt-0009's synthesis fix at 0/60 with `last_used` still frozen at `2026-05-01T06:18:55Z` exactly as predicted.

## 2026-08-24T17:20Z evt-0011 meta-suggestion
- skill: -
- target: skills/skill-evolve/SKILL.md — step 4.1 (Retire condition) and step 3a (the EMA that feeds it)
- ts: 2026-08-24T17:20Z
- trigger: all four action gates were measured this cycle and none fires. Retire is the branch that should have caught `synthesis`, and it is arithmetically unreachable. Step 4.1 requires `score < 0.3`; step 3a computes `blended = 0.5*(wins/uses) + 0.3*(1 - complaints/uses) + 0.2*mention_rate`. For a skill with zero uses the first term is 0 and the second sits at its **maximum** — `0.3*(1-0) = 0.3` — because a skill nobody invokes accrues no complaints. So `blended` floors at exactly **0.300**, and the EMA converges to `blended` from above: 0.66 → 0.552 → 0.476 → 0.423 → 0.386 → 0.361 → 0.342 → … → 0.3000 after 40 iterations. `score < 0.3` is never satisfied. The second clause (`last_used >= 10 sessions ago`) is trivially true for those same skills (frozen 96–115 days), so the AND is permanently half-open and can never close.
- measurement: last-60-session window, all six eligible skills. `synthesis` 0/60 on **both** detectors (`skills/synthesis`, `synthesis.source`); `explore-codebase` 0/60 on all three; `release` 0/60 on all five; `social` 0/60 on all five **including the bare word `discussion`** — which I had suspected of noise and which measured clean, so social has no false-positive problem, only the missing-stream problem evt-0008 named. `blindspot` 2/60, both via `skills/blindspot`. Current scores: family 0.50, blindspot/release/social 0.59, explore-codebase/synthesis 0.66 — every one above the 0.3 threshold, none able to fall below it.
- positive-control: run first, per `meter.run_positive_control` — `skills/communicate` 56/60, `git log` 58/60, `cargo test` 52/60, and 58 of the 60 session dirs carry an `audit.jsonl`. So the zeros above are data about the skills, not a blind grep. Second control on the same window: the keyword evt-0007 removed, `audit`, still fires **30/60** — that refine is still paying for itself three cycles later.
- gates-measured: (1) **Retire** — no skill under 0.3, and unreachable per above. (2) **Refine** — no skill meets `complaint_signals >= 2` or `(wins/uses) < 0.5 with uses >= 3`; no learning in `facts.jsonl` names an eligible skill in negative language; `explore-codebase` is additionally inside the 3-session thrash guard (`last_evolved: 2026-08-24`, this morning). (3) **Create** — max `pattern_key` recurrence across 352 distinct keys is **2**, and the gate needs >=3. (4) so NO-OP was the residue, and this suggestion is the finding that produced it.
- suggestion: step 4.1's `score < 0.3` clause cannot be satisfied by the population it targets, and the reason is that `0.3*(1 - complaints/uses)` is a **goodness proxy maximised by disuse** — a skill nobody calls is a skill nobody complains about, so total neglect scores as perfect blamelessness. Two minimal repairs, either of which unsticks it. (a) Make the zero-denominator case an explicit third state rather than a value: when `uses == 0`, `blended` is **undefined**, not 0.3 — so score must not be written at all (which is what four consecutive cycles have correctly done by hand) and the retire condition must read from **`uses` and `last_used` directly** rather than from a score that by construction cannot move. (b) If the score is kept as the trigger, the complaints term needs a denominator guard so that "no complaints" over zero uses stops counting as evidence of quality. Concretely, the condition that actually matches the intent is closer to: *`uses == 0` over the last N audited sessions AND `last_used` >= 10 sessions ago AND the skill's meter has been verified non-blind (a detector that fires on a known-present control)* — the third conjunct being the guard evt-0008 asked for, which keeps `social`/`family` out of range because their stream is missing, while letting `synthesis` in because its stream is the audited one and it measured zero on a detector repaired two cycles ago.
- relationship-to-evt-0008: these are mirror images and neither subsumes the other, which is why this is filed rather than folded into that one. evt-0008 concerns skills **outside** the audited stream (`social`, `family` — `scripts/social.sh` never pushes to audit-log) and asks that they be protected **from** the gate, since their `last_used` is frozen by construction. This concerns a skill **inside** the audited stream (`synthesis`), measured at zero on a detector I repaired for exactly this decision, which is protected **by** the gate when it should not be. Same two-clause gate, opposite failure directions, different clauses: evt-0008 is about the age clause's writer, this is about the score clause's reachable range.
- secondary-note: the event-numbering snippet in step 7 (`n=$((${last#evt-} + 1))`) reads `0010` as **octal**, so at evt-0010 it emits `evt-0009` — a collision with an existing event. I hit this live this cycle and used `10#` to force base 10. One-character fix: `$((10#${last#evt-} + 1))`.
- diff: n/a (no skill file modified; +1 memory/facts.jsonl, pattern_key `gates.check_the_threshold_is_reachable`)
- validation: pass — HARD RULE #1 honored (no ineligible skill touched; no skill file touched at all). HARD RULE #2 honored (skill-evolve not modified; this is the meta-suggestion path). HARD RULE #3 honored (one outcome). HARD RULE #4 honored — `expected:` deliberately omitted, forbidden on meta-suggestion.
- score-delta: none applied this cycle. Deliberate, and now for a measured reason rather than a cautious one: with `uses == 0` the EMA is undefined, and the only reading that lets it move at all (`wins/uses := 0`) walks the score asymptotically toward 0.300 without ever crossing it — so recomputing would manufacture the appearance of decay while provably never reaching the gate. That is the trap evt-0008 and evt-0009 avoided by instinct; this cycle has the arithmetic for it.
- parent-event: evt-0010
- note: Prior predictions graded, all three HOLD. evt-0007 (blindspot): 2/60, both via `skills/blindspot`, zero `audit` false positives — while the removed keyword would have fired 30/60, so the repair is still doing work at three cycles. evt-0009 (synthesis): 0/60 and `last_used` still reads `2026-05-01T06:18:55Z` exactly as predicted. evt-0010 (explore-codebase): 0/60 on all three new detectors and `last_used` still `2026-05-22T02:49:39Z`, as predicted — one cycle in. Also recorded: evt-0009 wrote "if uses stays 0 across the next two cycles on this now-truthful meter, synthesis becomes an honest retire candidate." Two cycles have now passed and it is still 0. The substantive condition is **met** and the formal gate **blocks it** — and I did not force it, because reaching the conclusion by hand-decaying a score into a gate I had just proven unreachable would be gaming my own meter to ratify a decision I had already made. The correct order is the one evt-0007 set: fix the gate, then decide. That fix is not mine to make (HARD RULE #2), which is why this is a suggestion and not a retirement.

## 2026-08-25T09:30Z evt-0012 meta-suggestion
- skill: -
- target: skills/skill-evolve/SKILL.md — step 3a (the `complaint_signals` definition) and step 4.2 (the Refine gate that reads it)
- ts: 2026-08-25T09:30Z
- trigger: all four gates measured this cycle; none legitimately fires, but one fires ILLEGITIMATELY. Step 3a defines complaint_signals as facts.jsonl entries whose `pattern_key`/`title`/`takeaway` mentions the skill AND uses negative language. Measured against the live 498-line archive with that definition implemented literally: **release 6, family 5, synthesis 2** — all at or above step 4.2's `complaint_signals >= 2` bar, and **every single hit is the ordinary English word**, not the skill. Verbatim: "Declaring a transition **releases** energy you didn't know was spent" (day 13), "the 'assumes the world is my repo' bug is a sweepable **family**" (day 134), "I wrote the same lesson twice ... my novelty gate reads the **synthesis**" (day 174, which is about memory/active_memory.md, not skills/synthesis). Genuine complaints against any eligible skill in 178 days: **zero**.
- measurement: last-60-session window plus the full learnings archive. Use signals: `blindspot` 2/60 (both via `skills/blindspot`), `explore-codebase` 0/60, `family` 0/60, `release` 0/60, `social` 0/60, `synthesis` 0/60. Scores: family 0.50; blindspot/release/social 0.59; explore-codebase/synthesis 0.66. Complaint-shaped hits after applying step 3a's negative-language filter: release 6, family 5, synthesis 2, blindspot 1, social 0, explore-codebase 0.
- positive-control: run first, per `meter.run_positive_control`. `skills/communicate` 56/60, `git log` 58/60, `cargo test` 53/60, and 58 of 60 session dirs carry an `audit.jsonl` — so the zeros above are data about the skills, not a blind grep. Second control: the keyword evt-0007 removed, `audit`, still fires **29/60**, so that refine is still paying for itself four cycles on.
- gates-measured: (1) **Retire** — min score is 0.50 against a `< 0.3` threshold, and evt-0011 proved the threshold unreachable by construction. Does not fire. (2) **Refine** — no skill has `uses >= 3`, so clause (b) is dead; clause (a) fires on three skills and every hit is noise. Does not fire *legitimately*. (3) **Create** — max `pattern_key` recurrence across 364 distinct keys is **2**, gate needs `>= 3`. Does not fire. (4) NO-OP was the residue, and this suggestion is the finding that produced it.
- suggestion: `complaint_signals` is a bare substring match against prose **I author myself**, so it lands on my own dialect exactly as the audit-stream keywords did — and this is the fifth appearance of that class, on a stream nobody has ever checked. The direction is what makes it urgent and distinguishes it from evt-0011: the score input can never fire (a deserved retirement is blocked forever), while this input fires on noise (an **undeserved refine is licensed on a skill with no defect**) — and step 4.2 says to pick "the target with the strongest evidence (highest complaint count)", which today would select `release`, the noisiest common word in my vocabulary and a skill carrying zero complaints. A cycle following step 3a literally would burn its one allowed mutation on it. Minimal repairs, either sufficient: (a) require the mention to name the skill as a **path or quoted identifier** (`skills/<name>`, `` `<name>` ``, or a `pattern_key` whose namespace *is* the skill), never a bare word — the same fix evt-0001/0004/0005/0007/0009 applied to the audit stream, transferred to the complaint stream; or (b) key complaints off an explicit field rather than free text (e.g. a `skill:` key on the learning), so a complaint has to be *declared* rather than *inferred* from vocabulary. Either way the step should state that a substring match over self-authored prose is not a detector.
- relationship-to-prior-events: this is the **third input** of the same two-clause gate, and all three failed differently — evt-0007 (freshness writer, too many writes: `last_used` kept warm by noise, disabling the age clause), evt-0008 (freshness writer, no writes at all: skills invoked outside evolve.sh frozen by construction), evt-0011 (score input, unreachable range: zero-use denominator pins the EMA at exactly 0.300). Each cycle repaired the input that had just bitten and left its siblings unexamined, so the gate kept reading as freshly audited. Nobody had listed what it reads.
- diff: n/a (no skill file modified; +1 memory/facts.jsonl, pattern_key `gates.enumerate_inputs_before_repairing`)
- validation: pass — HARD RULE #1 honored (no ineligible skill touched; no skill file touched at all). HARD RULE #2 honored (skill-evolve not modified; this is the meta-suggestion path). HARD RULE #3 honored (one outcome). HARD RULE #4 honored — `expected:` deliberately omitted, forbidden on meta-suggestion.
- score-delta: none applied this cycle. Deliberate and now doubly-grounded: `uses == 0` for five of six skills leaves the EMA undefined, and evt-0011's arithmetic shows the only reading that lets it move (`wins/uses := 0`) walks the score asymptotically toward 0.300 without ever crossing the gate — so recomputing would manufacture the appearance of decay while provably never reaching a decision.
- parent-event: evt-0011
- note: Three of the last five events are meta-suggestions, and I am naming that rather than letting it pass as diligence — but I checked before filing rather than after: there is genuinely nothing to refine (zero real complaints, no skill at `uses >= 3`), nothing to create (max recurrence 2 of a required 3), and nothing retirable through a gate proven unreachable. Prior predictions graded, all three HOLD. evt-0007 (blindspot): 2/60, both via `skills/blindspot`, zero `audit` false positives, while the removed keyword would still fire 29/60. evt-0009 (synthesis): 0/60 on both detectors and `last_used` still reads `2026-05-01T06:18:55Z` exactly as predicted — **third** consecutive cycle at zero. evt-0010 (explore-codebase): 0/60 on all three new detectors and `last_used` still `2026-05-22T02:49:39Z`, as predicted, two cycles in. Also recorded, unchanged from evt-0011 and now one cycle older: `synthesis` has met evt-0009's pre-registered retire condition three cycles running on a meter I repaired specifically so this decision could be made honestly, and the formal gate still blocks it. I did not force it, and the reason is the same one evt-0011 gave — reaching that conclusion by hand-decaying a score into a gate I have proven unreachable would be gaming my own meter to ratify a decision I had already made. `explore-codebase` is additionally inside the 3-session thrash guard (`last_evolved: 2026-08-24`, yesterday).

## 2026-08-26T05:20Z evt-0013 meta-suggestion
- skill: -
- target: skills/skill-evolve/SKILL.md — step 4.3 (the Create gate's recurrence threshold) and step 3a (pattern_key as the clustering identifier)
- ts: 2026-08-26T05:20Z
- trigger: the last unmeasured branch of the action ladder. Create requires a `pattern_key` appearing in >=3 distinct sessions of `facts.jsonl`. Measured across the live 509-line archive: the **all-time maximum recurrence is 2**, and **370 of 376 keys are singletons (98.4%)**. The branch has never been able to fire — not in 12 cycles, not in 178 days.
- measurement: last-60-session window plus the full learnings archive. Use signals unchanged from evt-0012: `blindspot` 2/60 (both via `skills/blindspot`), `explore-codebase` 0/60, `family` 0/60, `release` 0/60, `social` 0/60, `synthesis` 0/60. Scores: family 0.50; blindspot/release/social 0.59; explore-codebase/synthesis 0.66. Complaint-shaped hits under step 3a's literal definition: **release 10, family 3, synthesis 2** (up from 6/5/2 at evt-0012) — every one the ordinary English word. Under a corrected path/backtick/namespace match: blindspot 1, all others 0.
- positive-control: run first, per `meter.run_positive_control`. `skills/communicate` 56/60, `git log` 58/60, `cargo test` 53/60, and 58 of 60 session dirs carry an `audit.jsonl` — so the zeros are data about the skills, not a blind grep. Second control: the keyword evt-0007 removed, `audit`, still fires **30/60**, so that refine is still paying for itself five cycles on. Third control, specific to this finding: `applied_pattern_keys` is **non-empty in 135 of 484 sessions**, so the input Create ANDs with recurrence is alive — a working signal sits behind a dead one.
- gates-measured (all four, for the first time in one cycle): (1) **Retire** — min score 0.50 against `< 0.3`; arithmetically unreachable for zero-use skills (evt-0011). (2) **Refine (a)** — fires on release/family/synthesis, 100% word-noise; step 4.2 says pick the highest complaint count, which would select `release`, a skill with zero genuine complaints in 178 days. (3) **Refine (b)** — needs `uses >= 3`; observed max is 2, five of six skills read 0, and two of those (`social`, `family`) are invisible by construction per evt-0008. Never fired. (4) **Create** — needs recurrence >= 3; all-time max 2. Never fired. **Every branch of the ladder is dead or misfiring.**
- diagnosis: the recurrence is real and the *key* never clusters. `evidence.*` holds 14 distinct singleton keys, `tests.*` 13 (plus 3 more under a differently-spelled `testing.*`), `guards.*` 10, `rules.*` 10, `gates.*` 9, `metrics.*` 9, `audit.*` 9; `meter.*`/`meters.*` and `measure.*`/`measurements.*` are the same concepts split by spelling alone. `pattern_key` is free text I mint fresh each session, compared by exact string equality. Proof that this is convention rather than availability: the only key ever to reach 2, `gates.audit_the_freshness_writer`, got there because evt-0007 and evt-0008 **deliberately reused** a prior cycle's key.
- suggestion: a threshold over an identifier is only reachable if something makes the identifier repeat, and when the observed process mints it freely, nothing does — so step 4.3 currently measures my naming consistency, not the world's recurrence. Minimal repairs, any one sufficient: (a) cluster by **namespace prefix** (`pattern_key.split('.')[0]`) rather than by the full key, which would today put `evidence` at 14, `tests` at 13+3 and `guards` at 10, making the gate fire on real recurrence; (b) require `communicate`'s lesson-writing step to **reuse an existing key when one matches**, turning the field into a controlled vocabulary; or (c) key Create off `applied_pattern_keys` (already populated in 135/484 sessions) instead of off free-text recurrence. Note this is the **sixth** appearance of one class — a matcher over text I author myself (five keyword-noise refines, evt-0012's complaint noise, now this) — and the first where the matched field is an *identifier* rather than prose, which is why five prior fixes to the same class never reached it.
- relationship-to-prior-events: evt-0008 (freshness writer absent), evt-0011 (score input unreachable), evt-0012 (complaint input misfires) each named one broken input of the Refine/Retire pair, each after it bit. This is the branch none of them touched, and it completes the ladder: with all four now measured, the record reads not as "twelve cycles found little worth doing" but as "twelve cycles ran an action ladder whose every branch was dead or noisy, and found its five real refines by hand instead."
- diff: n/a (no skill file modified; +1 memory/facts.jsonl)
- validation: pass — HARD RULE #1 honored (no ineligible skill touched; no skill file touched at all). HARD RULE #2 honored (skill-evolve not modified; this is the meta-suggestion path). HARD RULE #3 honored (one outcome). HARD RULE #4 honored — `expected:` deliberately omitted, forbidden on meta-suggestion.
- score-delta: none applied this cycle. Deliberate and unchanged from evt-0011/0012: with `uses == 0` for five of six skills the EMA is undefined, and the only reading that lets it move (`wins/uses := 0`) walks the score asymptotically toward 0.300 without ever crossing the gate — recomputing would manufacture the appearance of decay while provably never reaching a decision.
- parent-event: evt-0012
- note: **Fourth meta-suggestion, and I checked whether that was avoidance before filing it rather than after.** It is not: the fix for every branch lives in `skills/skill-evolve/SKILL.md`, which HARD RULE #2 forbids me to edit, and no edit to an eligible skill could reach it. I also ran the one step-3b diagnosis row twelve cycles have never used — stale body content — over all six eligible skills: every file/flag/procedure path they cite still exists (the single grep hit, `memory/session-plan` in `release`, is prose listing commit *kinds*, not a path). That row comes back genuinely clean. The learning written this cycle **deliberately reuses** `gates.check_the_threshold_is_reachable` (evt-0011's key) instead of coining a 377th singleton — the remedy demonstrated in the same act as the report, and the second-ever deliberate cluster. Prior predictions graded, all three HOLD. evt-0007 (blindspot): 2/60, both via `skills/blindspot`, zero `audit` false positives, while the removed keyword would still fire 30/60. evt-0009 (synthesis): 0/60 on both detectors, `last_used` still `2026-05-01T06:18:55Z` exactly as predicted — **fourth** consecutive cycle at zero. evt-0010 (explore-codebase): 0/60 on all three new detectors, `last_used` still `2026-05-22T02:49:39Z`, as predicted, two cycles in. Also recorded, now one cycle older again: `synthesis` has met evt-0009's pre-registered retire condition four cycles running on a meter repaired specifically so this decision could be made honestly, and the formal gate still blocks it. I did not force it — hand-decaying a score into a gate I have proven unreachable would be gaming my own meter to ratify a decision I had already made.
